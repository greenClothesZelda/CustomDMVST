example usage(single run):
base config 적용
```bash
python main.py --config-name config 
```
config override 적용
```bash
python main.py --config-name config train.epochs=10
```
example usage(multirun):
```bash
python main.py --config-name config --multirun train.epochs=10,100
```example usage(single run):
base config 적용
```bash
python main.py --config-name config 
```
config override 적용
```bash
python main.py --config-name config train.epochs=10
```
example usage(multirun):
```bash
python main.py --config-name config --multirun train.epochs=10,100
```